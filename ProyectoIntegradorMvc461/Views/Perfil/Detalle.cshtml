@model ProyectoIntegradorMvc461.Models.Perfil

@{
    ViewBag.Title = "Detalle";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Detalle</h2>

<div>
    <h4>Perfil</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.id_perfil)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.id_perfil)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.t_perfil)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.t_perfil)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.f_estado)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.f_estado)
        </dd>

    </dl>
</div>
<p>
    @*@Html.ActionLink("Editar", "Editar", new { id = Model.id_perfil }) |
    @Html.ActionLink("Regresar a la lista", "Index")*@

    <button type="button" class="btn btn-success" onclick="location.href='@Url.Action("Editar", "Perfil", new { id = Model.id_perfil })'">Editar</button>
    <button type="button" class="btn btn-primary" onclick="location.href='@Url.Action("Index", "Perfil")'">Regresar a la Lista</button>

</p>
